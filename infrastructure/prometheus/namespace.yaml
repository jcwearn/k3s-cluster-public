apiVersion: v1
kind: Namespace
metadata:
  name: prometheus
  labels:
    # node-exporter is hostNetwork and hostPID by design -- that is how it sees
    # the node -- which baseline forbids.
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/warn: restricted
    pod-security.kubernetes.io/audit: restricted
