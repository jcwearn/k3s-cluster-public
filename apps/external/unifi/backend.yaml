apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: unifi
  namespace: unifi
spec:
  endpoints:
    - ip:
        address: ${MGMT_PREFIX}.1
        port: 443
  tls:
    insecureSkipVerify: true
    alpnProtocols:
      - http/1.1
