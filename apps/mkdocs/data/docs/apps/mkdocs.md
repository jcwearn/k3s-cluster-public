# MkDocs (cluster docs)

This very documentation site!

* Built with **MkDocs + Material** in the repo’s `docs/` folder.
* Deployed by GitHub Pages (not via the cluster).
* Theme tweaks live in `mkdocs.yml`.

> **TODO** – if you ever move the site *into* the cluster (e.g. via
> `ghcr.io/squidfunk/mkdocs-material` Docker image), document the helm/kustomize here.