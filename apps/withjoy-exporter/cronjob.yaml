apiVersion: batch/v1
kind: CronJob
metadata:
  name: withjoy-exporter
  namespace: withjoy-exporter
spec:
  schedule: "0 6 * * *"
  timeZone: America/New_York
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: 1800
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: withjoy-exporter
              image: ghcr.io/jcwearn/withjoy-exporter:v0.4.18@sha256:4e04da38c258658e2daec007db59c728d808ca39505f77c425b0a54d3245548e
              imagePullPolicy: IfNotPresent
              envFrom:
                - secretRef:
                    name: withjoy-exporter-env
                - configMapRef:
                    name: withjoy-exporter-config
              volumeMounts:
                - name: google-credentials
                  mountPath: /secrets
                  readOnly: true
              resources:
                requests:
                  cpu: 200m
                  memory: 1Gi
                limits:
                  cpu: "1"
                  memory: 2Gi
          volumes:
            - name: google-credentials
              secret:
                secretName: withjoy-exporter-google-credentials
                defaultMode: 0400
                items:
                  - key: google-service-account.json
                    path: google-service-account.json
