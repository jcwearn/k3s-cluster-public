apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: homepage
  namespace: homepage
spec:
  endpoints:
    - dnsName: ${DOMAIN}
      recordType: A
      recordTTL: 300
      targets:
        - "100.91.140.56"
